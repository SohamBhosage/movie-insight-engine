/*import { generateSentiment } from "./sentiment";

describe("Sentiment Engine", () => {

  test("returns Positive for high rating and high votes", () => {
    const result = generateSentiment(8.5, 100000);
    expect(result.classification).toBe("Positive");
  });

  test("returns Mixed for moderate rating", () => {
    const result = generateSentiment(6.5, 20000);
    expect(result.classification).toBe("Mixed");
  });

  test("returns Negative for low rating", () => {
    const result = generateSentiment(4.5, 5000);
    expect(result.classification).toBe("Negative");
  });

});*/